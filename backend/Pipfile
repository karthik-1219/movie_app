[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
Flask = "==3.1.0"
flask-cors = "==5.0.0"
gunicorn = "==23.0.0"

[dev-packages]
pytest = "==8.3.4"
flake8 = "==7.1.1"

[scripts]
serve = "python -m backend"
test = "pytest -q"
lint = "flake8 ."

[requires]
python_version = "3.10"
